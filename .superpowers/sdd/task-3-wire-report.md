# Task 3 Wire Review Fix

## RED

- Command: `cd backend && go generate ./cmd/server`
- Initial local failure: Wire v0.7.0 required missing `github.com/google/subcommands` checksums. Running the generator directive's exact command (`go run -mod=mod github.com/google/wire/cmd/wire`) added the two required `backend/go.sum` entries.
- Reproduced Wire error: no provider for `*service.GatewayService`, required by `ProvideOpsService`; no provider for `*service.BatchImageWorkerRuntime`, required by `provideCleanup`.

## Root Cause

- `service.ProviderSet` omitted existing `NewGatewayService` and `ProvideBatchImageWorkerRuntime` providers.
- `wire_gen.go` already contained both generated construction calls, proving the source provider declaration had drifted from the last successful generated dependency graph.
- Upstream commit `a994fbd77` added both entries with the Batch Image feature; they are absent from the merged current provider set.

## Assumption

- The constructors' existing behavior is the intended startup behavior; restoring their provider declarations is compatibility repair only.

## Fix

- Added `NewGatewayService` and `ProvideBatchImageWorkerRuntime` to `service.ProviderSet`.
- Retained the checksum entries generated by the repository's `-mod=mod` Wire command.

## GREEN

- `cd backend && go generate ./cmd/server` completed successfully.
- A second generation produced the same `wire_gen.go` blob hash: `9a8199f88b7c06b2df1c71ccdd7c753eac842f79`.
- `cd backend && go test ./cmd/server -run '^$'` passed.
- `cd backend && go test ./internal/service -run '^TestGatewayService_'` passed.
- `cd backend && go test -tags unit ./internal/service -run '^TestBatchImageWorkerRuntime_'` could not build unrelated existing unit tests: missing `customMenuIDsToResourceIDs`, `adminServiceImpl.openaiProbeControl`, and `adminServiceImpl.applyProbeToggleSideEffects`.

## Generated Diff

- `wire_gen.go` was not manually edited. Its generated diff restores the Batch Image worker construction, moves Batch Image construction nearer to its handler use, and updates the stale `ProvideUserPlatformQuotaUsageFlusher` call to the current `NewUserPlatformQuotaUsageFlusher` declaration already present in `service.ProviderSet`.
