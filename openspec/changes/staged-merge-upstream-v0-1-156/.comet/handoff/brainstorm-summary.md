# Brainstorm Summary

- Change: staged-merge-upstream-v0-1-156
- Date: 2026-07-16

## 确认的技术方案

- 最终目标为上游正式 tag `v0.1.156`，不包含 release 后的 `upstream/main` 提交。
- 采用同一 change 内的阶段 0 强制测试门禁，再依次合入 `v0.1.152`、`v0.1.153`、`v0.1.155`、`v0.1.156`。
- 本地首 Token 超时是唯一允许完整移除的能力；用户已知上游只部分覆盖且仍选择完全采用上游语义。
- 其余本地能力全部保护；不可共存语义必须暂停等待用户决定。
- 不推送、不发布、不部署，不做无关重构。
- 每个 tag 形成独立 `--no-ff` merge 节点；该阶段兼容修复和验证完成后才能继续。
- 阶段 0 的测试补齐先形成普通提交；每个 tag 的冲突解决保留在对应 merge commit，merge 后发现的语义回归使用独立普通提交修复。
- 任一阶段存在未授权且无法共存的业务语义时停止，不创建下一 tag 的 merge 节点。
- 阶段 0 先运行现有全量门禁，再按“本地独有能力 × 上游 changed files × 现有测试”找出高风险缺口，仅添加最小回归测试。
- 每段同时维护冲突台账和能力矩阵，覆盖有文本冲突与无文本冲突的语义变化。
- 能力矩阵来源为主 OpenSpec、本地知识库、上次合并验证矩阵和 `v0.1.151..HEAD` 本地提交；每行记录入口、关键文件、受影响 tag、自动测试、人工审查点和阶段结果。
- 能力状态限定为 `protected`、`gap`、`manual`、`approved-removal`；仅首 Token 可在 `v0.1.156` 阶段进入 `approved-removal`。
- 阶段 0 只为本次目标实际触及的高风险行为补最小测试，不追求逐行覆盖或建立新测试框架。
- 最终阶段重新生成并复核 Wire/Ent，逐个检查 migration、版本、依赖和配置默认值。

## 关键取舍与风险

- 阶段验证采用固定本地保护集加 changed-files 定向测试，最终才运行完整门禁，避免四次重复完整测试造成无意义等待。
- 风险：分段 tag 仍可能重复冲突；能力矩阵只能降低漏检概率，不能替代人工语义审查。

## 测试策略

- 阶段 0：当前 `HEAD` 全量本地门禁、前端 build、生成代码检查；补齐高风险行为缺口后重跑。
- 每个 tag：全部本地保护测试、changed-files 对应包/组件测试、冲突相关测试、静态冲突残留检查。
- 最终：完整本地门禁、前端 build、Wire/Ent 可复现生成、migration、Git 祖先、能力矩阵和 thorough review。

## Spec Patch

无。
