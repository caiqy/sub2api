## 1. 指标采集

- [ ] 1.1 扩展 OpsMetricsCollector，采集 runtime heap、RSS/cgroup memory、swap 或可用降级指标。
- [ ] 1.2 在网关请求处理路径记录请求体大小分桶和大请求计数，不记录正文。
- [ ] 1.3 在内容审计截断和 SSE 行超限路径记录风险计数。
- [ ] 1.4 暴露 usage 记录池水位指标，包括 waiting、workers、sync fallback、drop。

## 2. 告警与后台

- [ ] 2.1 设计并实现内存压力告警阈值配置，包含冷却时间和恢复阈值。
- [ ] 2.2 接入现有通知服务发送触发和恢复通知。
- [ ] 2.3 增加管理端查看入口，展示当前内存压力、近期大请求和最近告警。

## 3. 验证

- [ ] 3.1 增加采集失败降级测试，覆盖 cgroup 不可用场景。
- [ ] 3.2 增加告警触发、冷却和恢复测试。
- [ ] 3.3 手工验证后台展示不包含请求正文或图片内容。
