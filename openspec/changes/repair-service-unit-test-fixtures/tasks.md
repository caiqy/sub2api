## 1. Credits fixture

- [x] 1.1 为 credits overages direct-call fixture 初始化并清理 `payloadHandle`，运行聚焦测试。

## 2. Pricing fixture

- [ ] 2.1 对齐 channel/group 平台并删除重复 image output pricing 用例，运行定价聚焦测试。

## 3. Settings fixture

- [ ] 3.1 让 settings repo stub 实现 `GetValue` 缺失语义并恢复全局状态，运行 settings 聚焦测试。
